#---keyword arguments---
    #these arguments are passed to the function with name-value pair so keyword arguments can identify the firmal arguments by their names.the keyword argument's name and formal arguments's name must match.
#------------------------------------------------------------------------
def show(name,age):
    print(name,age)
show(age=62,name="surya")
#in this order of key value pair is not nessacery to match with formal arguments(parameter).
#note:- number of formal argument and actual argument(specifically  here keyword argument)must be same.

