#---types of actual arguments---
    #1 positional arguments
    #2 keyword arguments
    #3 default arguments
    #4 variable length arguments
    #5 keyword variable length arguments
#---positional arguments--- 
    #these arguments are passed to the function in correct position order.the number of arguments and their postions in the function defination should be equal ti the number and position of the argument in the function call.
#------------------------------------------------------------------------
def pw(x,y):
    z=x**y
    print(z)
pw(5,2)
#note:-number of argument and number of parameter must be same.
#-------------------------------END--------------------------------------